/* Crea una función llamada determine que reciba como parámetro cualquier valor. La función debe retornar "string" "boolean" o "number" según el tipo de dato que reciba como parámetro. */

function determine (valor) {
    return typeof (valor);
}