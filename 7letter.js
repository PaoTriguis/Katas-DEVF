/* Crea una función llamada letter ingresar cualquier letra del alfabeto y verifique si es vocal o consonante
"Vocal" si la letra es una vocal.
"Consonante" si la letra es una consonante. */

function letter (texto) {
    if (letter = "a" || "e" || "i" || "o" || "u") {
        return ("Vocal");
    }
    else {
        return ("Consonante");
    }
}